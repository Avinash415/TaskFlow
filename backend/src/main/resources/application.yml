# spring:
#   application:
#     name: taskflow-backend

#   datasource:
#     url: ${DB_URL}
#     username: ${DB_USERNAME}
#     password: ${DB_PASSWORD}
#     driver-class-name: org.postgresql.Driver

#   jpa:
#     hibernate:
#       ddl-auto: update
#     show-sql: false
#     properties:
#       hibernate:
#         format_sql: true
#         dialect: org.hibernate.dialect.PostgreSQLDialect

#   flyway:
#     enabled: true
#     baseline-on-migrate: true

# server:
#   port: ${PORT:8080}

# jwt:
#   secret: ${JWT_SECRET}
#   expiration: 86400000
#   refresh-expiration: 604800000

# app:
#   frontend-url: ${FRONTEND_URL}  


spring:
  application:
    name: taskflow-backend

  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/taskflow}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:postgres}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

  flyway:
    enabled: true
    baseline-on-migrate: true

server:
  port: ${PORT:8080}

jwt:
  secret: ${JWT_SECRET:ThisIsMyLocalJWTSecretKey12345678901234567890}
  expiration: 86400000
  refresh-expiration: 604800000

app:
  frontend-url: ${FRONTEND_URL:http://localhost:5173}